- [x] Detect 404: `/admin` route is missing (navigation points to `/admin`, but only `/admin/chat` and `/admin/delivery` exist).
- [x] Fix missing `/admin` route by adding `src/app/admin/page.tsx` (redirect to `/admin/chat`).
- [x] Re-run `npm run build`.
- [ ] Verify `/admin` loads (and links to chat/delivery).

